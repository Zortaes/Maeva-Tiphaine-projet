<div id="overlay" style="display: none;"></div>

        <nav class="navbar navbar-expand-lg navbar-light">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
                <div class="titreNav"><h3>Titre du site</h3></div>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                <button class="nav-item" href="#">Accueil</button>
                    <button class="toggleCategories" data-toggle="collapse" data-target="#navbarToggleExternalContent" aria-controls="navbarToggleExternalContent" aria-expanded="false" aria-label="Toggle navigation">
                        Catégories
                    </button>
                <button class="nav-item" href="#">Nous contacter</button>
                    </div>
                </div>
                <div class="connexionBtn">
                    <button class="connexion" href="#">Connexion</button>
                </div>
            </div>                    
        </nav>
                <div class="pos-f-t">
            <div class="collapse" id="navbarToggleExternalContent">
                <div class="bg-light">
                    <div id= "categoriestoggled">
                        {% for category in categories %}
                            <a href="#">{{ category.name }}</a>
                        {% endfor %}
                    </div>     
                </div>
            </div>

        <div id="mySidebar" class="sidebar">
        <div id="bonjourSidebar">
        <button class="closebtn">&times;</button>
        <h5>Bonjour, veuillez vous identifier</h5>
        </div>
            <h5 id="categoriesNav">Catégories</h5> 
            <div id="sideList">   
                        {% for category in categories %}
                            <a class="sidebarCategorie" href="#">{{ category.name }}</a>
                        {% endfor %}
                <a href="#"><h5>Nous contacter</h5></a>
                <a href="#"><h5>Se connecter</h5></a>
                <a href="#"><h5>S'inscrire</h5></a> 
                    
            </div>
        </div>