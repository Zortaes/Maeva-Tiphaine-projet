<div id="overlay" style="display: none;"></div>

    <div id="siteNavbar">
        <button id="sideOpenbtn">
        <div class="line1"></div>
        <div id="line2"></div>
        <div class="line1"></div>
        </button>
        <a id="navTitle" href="#"><h3>Titre du site</h3></a>
        <div id="linkList">
        <button class="navLinks" href="#" >Accueil</button>
        <div class="subnav">
            <button class="navLinks" id="categoryNavBtn" href="#" >Catégories</button>
            <div class="show">   
                {% for category in categories %}
                    <a class="sidebarCategorie" href="#">{{ category.name }}</a>
                {% endfor %}
            </div>
        </div>
        <button class="navLinks" href="#" >Nous contacter</button>
        </div>
        <button id="connexionBtn" href="#">Connexion</button>
    </div>

    <div id="mySidebar" class="sidebar">
        <div id="bonjourSidebar">
        <button class="closebtn">&times;</button>
        <h5>Bonjour, veuillez vous identifier</h5>
        </div>
        <h5 id="categoriesNav">Catégories</h5> 
        <div id="sideList">   
            {% for category in categories %}
                <a class="sidebarCategorie" href="#">{{ category.name }}</a>
            {% endfor %}
            <a class="sideLinks" href="#"><h5>Nous contacter</h5></a>
            <a class="sideLinks" href="#"><h5>Se connecter</h5></a>           
            <a class="sideLinks" href="#"><h5>S'inscrire</h5></a>                    
        </div>
    </div>